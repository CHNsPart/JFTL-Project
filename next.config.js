/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  devIndicators: {
    
  },
  reactStrictMode: false,
};
module.exports = nextConfig;


// next.config.js for production with next 13